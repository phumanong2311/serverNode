import { Router } from "express";

const router = Router();
//Login route
// router.post("/getAll", AuthController.login);

//Change my password
// router.post("/change-password", [checkJwt], AuthController.changePassword);

export default router;
